# dépendances
node_modules/

# build Next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# environnement — jamais versionné (CLAUDE.md, règle 6)
.env
.env.local
.env*.local

# adaptateurs locaux
.mails/
.devclock.json

# Supabase local
supabase/.temp/
supabase/.branches/

# artefacts d'ingestion
.ingestion/

# Contenu éditorial — volontairement hors dépôt.
# 36 Mo aujourd'hui, et la spécification (§7.4.6) prévoit ~20 Mo par titre :
# un catalogue de 40 titres approcherait le gigaoctet. Ce sont des livrables du
# client, pas du code. La chaîne d'ingestion (étape 7) sera testée sur un
# fichier de test réduit, versionné dans tests/fixtures/.
conte d'afrique/

# divers
.DS_Store
Thumbs.db
# Réglages d'affichage de dossier que l'Explorateur Windows écrit tout seul.
desktop.ini
coverage/
*.log

# Maquettes de reference, livrees par l outil de design.
# 1,3 Mo de HTML autonome : une intention visuelle, pas du code deploye.
update_design/

# Captures d ecrans : 23 Mo de PNG regenerables par scripts/captures-ecrans.mjs.
captures/

# Dossier de passation Claude Design : 7,6 Mo de prototypes, d images et de
# documents. Meme traitement qu update_design/ — c est une intention visuelle,
# pas du code deploye, et la source fait foi dans le projet Claude Design
# b41ce6a2-73cb-4d17-bb4f-8582be43d7e0. La decision, elle, est versionnee
# dans docs/REFONTE-V3.md.
design_handoff_edition_mapoukam/
.captures/

.vercel
.env*

# Exports du Supabase hébergé : données réelles (comptes, commandes), jamais versionnées.
exports-supabase/
